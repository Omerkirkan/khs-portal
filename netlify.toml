[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (public/_redirects ile de sağlanır; burada yedek olarak tutulur)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
